export enum HorizontalType {
    LEFT = 'left',
    CENTER = 'center',
    RIGHT = 'right',
    BETWEEN = 'between',
    EVENLY = 'evenly',
    STRETCH = 'stretch',
}

export enum VerticalType {
    TOP = 'top',
    MIDDLE = 'middle',
    BOTTOM = 'bottom',
    BASELINE = 'baseline',
}

export enum TextType {
    LEFT = 'text-left',
    CENTER = 'text-center',
    RIGHT = 'text-right',
    JUSTIFY = 'text-justify',
}

export enum GapType {
    SM = 'sm',
    MD = 'md',
    LG = 'lg',
    XL = 'xl',
    XXL = 'xxl',
}